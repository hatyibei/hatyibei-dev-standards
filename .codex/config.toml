# Codex project configuration
# AGENTS.md is auto-discovered at repo root

[project]
# AGENTS.md の最大読み込みサイズ (default: 32KiB)
project_doc_max_bytes = 32768

[features]
multi_agent = true

[agents]
max_threads = 4
max_depth = 1
